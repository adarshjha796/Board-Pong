using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class GameManager : MonoBehaviour
{
    /*public TextMeshProUGUI gameOverText;

    bool gameOver;

    //public GameObject[] balls;
    public Transform decider;

    public int BallsWithPlayerOne;

    public int BallsWithPlayerTwo;

    public Behaviour[] scriptsToDisable;

    private void Start()
    {
        gameOver = false;
    }

    void Update()
    {
        if(balls[0].transform.position.y > decider.position.y)
        {
            BallsWithPlayerOne--;
        }

        if(balls[1].transform.position.y < decider.position.y)
        {
            BallsWithPlayerTwo--;
        }

        if(BallsWithPlayerOne == 0 && !gameOver)
        {
            gameOver = true;
            //GameOverForPlayerOne();
        }
        if (BallsWithPlayerTwo == 0 && !gameOver)
        {
            gameOver = true;
            //GameOverForPlayerTwo();
        }
    }

    private void GameOverForPlayerOne()
    {
        gameOver = false;
        gameOverText.text = "Player 1 Won";
        for (int i = 0; i < scriptsToDisable.Length; i++)
        {
            scriptsToDisable[i].enabled = false;
        }
    }

    private void GameOverForPlayerTwo()
    {
        gameOver = false;
        gameOverText.text = "Player 2 Won";
        for (int i = 0; i < scriptsToDisable.Length; i++)
        {
            scriptsToDisable[i].enabled = false;
        }
    }*/
}
